<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/noteListFragment">

    <fragment
        android:id="@+id/noteListFragment"
        android:name="com.dsvag.keepyournote.ui.fragments.NoteListFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_note_list">
        <action
            android:id="@+id/action_noteListFragment_to_noteFragment"
            app:destination="@id/noteFragment" />
    </fragment>

    <fragment
        android:id="@+id/noteFragment"
        android:name="com.dsvag.keepyournote.ui.fragments.NoteFragment"
        android:label="Note"
        tools:layout="@layout/fragment_note">
        <action
            android:id="@+id/action_noteFragment_to_noteListFragment"
            app:destination="@id/noteListFragment" />
        <action
            android:id="@+id/action_noteFragment_to_labelFragment"
            app:destination="@id/labelFragment" />
    </fragment>

    <fragment
        android:id="@+id/labelFragment"
        android:name="com.dsvag.keepyournote.ui.fragments.LabelFragment"
        android:label="Pick label"
        tools:layout="@layout/fragment_label">
        <action
            android:id="@+id/action_labelFragment_to_noteFragment"
            app:destination="@id/noteFragment" />
    </fragment>

</navigation>